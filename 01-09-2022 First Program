#include <stdio.h>
#include <stdlib.h>
int Num(int num);
int main()
{
int num,res;
printf("Enter a number\n");
scanf("%d",&num);
res=Num(num);
if(res==0)
printf("Even");
else
printf("Odd");
return 0;
}
int Num(int num)
{
if(num%2==0)
return (0);
else
return (1);
}
